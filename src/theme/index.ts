export { colors } from "./colors";
export { typography } from "./typography";
export { fonts } from "./fonts";
export { spacing, radius } from "./spacing";
export { messages } from "./messages";
export { cardShadow, elevatedShadow, navShadow } from "./shadows";
export { feedRadius, feedCardShadow, feedNavShadow, feedAvatarRing } from "./feedStyles";

import { colors } from "./colors";
import { typography } from "./typography";
import { fonts } from "./fonts";
import { spacing, radius } from "./spacing";
import { messages } from "./messages";

/** Centralized Premium theme (colors, typography, spacing, messages) */
export const theme = { colors, typography, fonts, spacing, radius, messages };
